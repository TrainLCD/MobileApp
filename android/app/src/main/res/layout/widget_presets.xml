<?xml version="1.0" encoding="utf-8"?>
<!--
  アプリ内で登録したプリセットを並べるウィジェット。
  iOS版のPresetsWidgetと同じ体裁で、
  ヘッダー + 「路線色バッジ / プリセット名 / 始発駅→終着駅」の行で構成する。

  面は乗車中ウィジェットと同じグラデーションだが、プリセットごとに路線が違うため
  下地の色はブランドカラーで固定し、路線色は行のバッジで見せる
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_presets_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@null"
        android:scaleType="fitXY"
        android:src="@drawable/widget_background"
        android:tint="@color/widget_brand" />

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@null"
        android:scaleType="fitXY"
        android:src="@drawable/widget_background_shade" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="10dp">

        <!-- iOS版のLineColorChip相当。面がブランドカラー固定なので色は静的に決め打ちできる -->
        <FrameLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content">

            <ImageView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:alpha="0.18"
                android:contentDescription="@null"
                android:scaleType="fitXY"
                android:src="@drawable/widget_chip_background" />

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center_vertical"
                android:orientation="horizontal"
                android:paddingStart="8dp"
                android:paddingTop="3dp"
                android:paddingEnd="8dp"
                android:paddingBottom="3dp">

                <ImageView
                    android:layout_width="12dp"
                    android:layout_height="12dp"
                    android:layout_marginEnd="4dp"
                    android:contentDescription="@null"
                    android:src="@drawable/ic_notification_live_update"
                    android:tint="@color/widget_on_line_primary" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/widget_presets_title"
                    android:textColor="@color/widget_on_line_primary"
                    android:textSize="11sp"
                    android:textStyle="bold" />
            </LinearLayout>
        </FrameLayout>

        <!--
          行はPresetsWidgetServiceがアダプタとして供給する。
          高さに入るぶんだけ表示され、残りはスクロールで辿れる
        -->
        <ListView
            android:id="@+id/widget_presets_list"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginTop="6dp"
            android:layout_weight="1"
            android:divider="@null"
            android:dividerHeight="0dp" />

        <!-- setEmptyViewでListViewと排他に切り替わる -->
        <TextView
            android:id="@+id/widget_presets_empty"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginTop="6dp"
            android:layout_weight="1"
            android:gravity="center_vertical"
            android:text="@string/widget_presets_empty"
            android:textColor="@color/widget_on_line_secondary"
            android:textSize="12sp" />
    </LinearLayout>
</FrameLayout>
