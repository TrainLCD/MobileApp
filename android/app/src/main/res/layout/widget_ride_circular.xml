<?xml version="1.0" encoding="utf-8"?>
<!-- 1セル幅で文字が入らないサイズ向け。watchOS/ロック画面のaccessoryCircular相当 -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/widget_background"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@null"
        android:scaleType="fitXY"
        android:src="@drawable/widget_background_circle"
        android:tint="@color/widget_brand" />

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@null"
        android:scaleType="fitXY"
        android:src="@drawable/widget_background_circle_shade" />

    <!--
      面そのものが路線色のバッジになるため、他のレイアウトのようなナンバリングバッジは重ねず、
      記号だけを面の前景色で載せる
    -->
    <TextView
        android:id="@+id/widget_line_symbol"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:ellipsize="end"
        android:maxLines="1"
        android:paddingStart="10dp"
        android:paddingEnd="10dp"
        android:text="@string/widget_line_symbol_placeholder"
        android:textColor="@color/widget_on_line_primary"
        android:textSize="20sp"
        android:textStyle="bold" />
</FrameLayout>
