/**
 * 低消費電力テーマ(#3697)の配色と基準寸法。
 *
 * 情報の区別は色相ではなく明度で行う。5段階の輝度差はモノクロへ落としても
 * そのまま残るため、電子ペーパーのような単色表示でも同じ読み取りができる。
 * 括弧内は背景(純黒)に対するコントラスト比。
 */
export const LOW_POWER_THEME_COLORS = {
  /** 背景。有機ELでは画素が消灯する純黒 */
  background: '#000000',
  /** 駅名・行先などの主情報 (21.0:1) */
  primary: '#FFFFFF',
  /** ローマ字・駅番号・到着分などの副情報 (7.5:1) */
  secondary: '#9A9A9A',
  /** 罫線・軌道・通過済みの駅 (4.1:1) */
  muted: '#6E6E6E',
  /** 状態ラベルと列車位置。モノクロでは #BEBEBE 相当へ落ちる (11.5:1) */
  accent: '#FFB000',
} as const;

/**
 * レイアウトを起こした基準画面の短辺(dp)。Pixel 3 の横向き実寸 720x360 に合わせてある。
 * 各寸法はこの値に対する実機短辺(セーフエリアを除いた実効値)の比率で拡縮するので、
 * 低解像度端末では設計時とドット単位で同じ見た目になる。
 */
export const LOW_POWER_BASE_HEIGHT = 360;
